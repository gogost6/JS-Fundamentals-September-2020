function solve(params) {
    let paramsUpper = params.toUpperCase();
    if (params == paramsUpper) {
        console.log(`upper-case`);
    } else {
        console.log(`lower-case`);
    }
}
solve(`l`)
